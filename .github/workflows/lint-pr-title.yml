name: 'Lint PR title'

on:
  pull_request:
    branches:
      - develop
    types: [opened, edited, synchronize]

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: deepakputhraya/action-pr-title@v1.0.2
        with:
          regex: '^(\w*)(?:\((.*)\))?: (.*)$'
