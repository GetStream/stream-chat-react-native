export const defaultAutoCompleteSuggestionsLimit = 10;
export const defaultMentionAllAppUsersQuery = {
  filters: {},
  options: {},
  sort: {},
};
export const POLL_OPTION_HEIGHT = 56; // (48px + 8px) 48 is the height of the input and 8 is the spacing between each option.
export const ONE_SECOND_IN_MILLISECONDS = 1000;
