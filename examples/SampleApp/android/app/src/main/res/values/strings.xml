<resources>
    <!-- app_name is generated by `resValue` in build.gradle so the RC build can carry a
         different label. Declaring it here too would be a duplicate-resource error. -->
</resources>
