Text for button within [`AttachmentPickerError`](../../core-components/overlay-provider#attachmentpickererror), which when pressed opens the Settings app and displays the app’s custom settings.

| Type | Default |
| - | - |
| string | "Allow access to your Gallery" |