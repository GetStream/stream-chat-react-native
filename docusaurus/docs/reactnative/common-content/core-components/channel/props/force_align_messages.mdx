Forces alignment for messages in list either on left side or right side of screen. By default, received messages get aligned on left side of screen and sent messages go to right side of screen.

| Type | Default |
| - | - |
| enum('left', 'right') | false |
