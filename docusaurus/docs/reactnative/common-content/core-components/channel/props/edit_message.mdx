Override of the *Edit Message* [`MessageAction`](/reactnative/object-types/message-action) shown when a user long presses a message.
The `editMessage` function receives as a parameter the relevant message and must return a `MessageAction`.

| Type |
| - |
| function \| null |
