UI component to render reaction list within message overlay, which shows up when user long presses a message.

| Type | Default |
|- -|- -|
| Component | [OverlayReactionList](/reactnative/ui-components/overlay-reaction-list) |
