Function which returns true if passing nextMessage to render would return the same result as passing prevMessage to render, otherwise return false.

| Type     |
| -------- |
| function |

| Parameter   | Description                     |
| ----------- | ------------------------------- |
| prevMessage | previous message to be compared |
| nextMessage | next message to be compared     |
