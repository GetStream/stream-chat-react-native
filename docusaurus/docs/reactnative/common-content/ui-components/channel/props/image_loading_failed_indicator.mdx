Component to render when image fails to load in `Gallery`.

| Type          | Default                       |
| ------------- | ----------------------------- |
| ComponentType | `ImageLoadingFailedIndicator` |
