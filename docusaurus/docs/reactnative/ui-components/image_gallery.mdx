---
id: image-gallery
title: ImageGallery
---

import ImageGalleryCustomComponents from '../common-content/ui-components/overlay-provider/props/image_gallery_custom_components.mdx';

Component to render the image viewer. This component handler all the gestures for zooming and swiping on images within gallery.

Its not possible to replace this component, given the complexity of it. But you can use `imageGalleryCustomComponents` prop on [`OverlayProvider`](../overlay-provider) to replace sub-parts of image gallery.

## Props

### <div class="label description">_forwarded from [OverlayProvider](../overlay-provider#imagegallerycustomcomponents) props_</div> `imageGalleryCustomComponents` {#imagegallerycustomcomponents}

<ImageGalleryCustomComponents />
